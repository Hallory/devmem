[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "devmem"
version = "0.1.0"
dependencies = []

[project.scripts]
devmem = "app.cli:main"

[tool.setuptools.packages.find]
include = ["app*"]